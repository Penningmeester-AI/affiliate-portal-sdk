name: test_app
description: Internal QA test app for Afflicate SDK. Not published.
publish_to: 'none'

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.3.2
  http: ^1.2.0
  affiliate_portal_sdk:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
